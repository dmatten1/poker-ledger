const addAllTimeButton = document.getElementById('addToAllTime');
const entrySet = localStorage.getItem('entrySet');

addAllTimeButton.addEventListener('click', () => {
    //add to the big total
  
  
  
  
  
    window.location.href = 'index.html';
  
  })